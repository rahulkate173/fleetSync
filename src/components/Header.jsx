import React from "react";

const Header = () => {
  return (
    <div className="header">
      <input type="text" placeholder="Search..." />
      <button>☰</button>
    </div>
  );
};

export default Header;
